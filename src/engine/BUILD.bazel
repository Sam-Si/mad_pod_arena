load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "engine",
    srcs = ["engine.cpp", "arena.cpp"],
    hdrs = ["engine.h", "arena.h", "bot.h"],
    deps = [
        "//src/core:core_headers",
        "//src/physics:physics",
    ],
    visibility = ["//visibility:public"],
)
