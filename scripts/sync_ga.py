import shutil
shutil.copy("cg_bot.cpp", "ga_bot.cpp")
# ga_bot.cpp is usually just cg_bot.cpp with main removed or something?
# wait, earlier ga_bot.cpp was a separate test file. Let's see what ga_bot.cpp actually is.
