module(
    name = "mad_pod_arena",
    version = "1.0",
)

# Pin to the version resolved by Bazel 9 / BCR (avoids direct-dep mismatch warnings).
bazel_dep(name = "rules_cc", version = "0.2.17")
bazel_dep(name = "nlohmann_json", version = "3.11.3")
